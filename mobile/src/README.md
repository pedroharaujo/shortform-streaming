# Mobile source placeholder

P1-T03 owns the strict TypeScript source layout. It will add `api`, `features`, `components`, `analytics`, and test boundaries together with working lint, type, and test configuration.
